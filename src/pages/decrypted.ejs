<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <title>encrypt</title>
    <style>
      body {
        font-family: sans-serif;
      }

      .max-w-3xl {
        box-shadow: 0 10px 30px 0 rgba(128, 0, 128, 0.5);
      }

      button {
        box-shadow: 0 10px 30px 0 rgba(128, 0, 128, 0.5);
        background-color: #450445;
      }

      textarea {
        background-color: rgba(0, 0, 0, 0.963);
      }
    </style>
  </head>
  <body class="bg-black text-white h-screen flex items-center justify-center">
    <div class="flex flex-col items-center">
      <div
        class="max-w-3xl p-8 bg-black rounded-lg border-2 border-white shadow-lg"
      >
        <h1 class="text-4xl font-bold mb-4">You've received a message.</h1>
        <p class="text-gray-500 mb-6">
          End to end encrypted. Only you and the receiver can read the message,
          deleted after the first read.
        </p>
        <div class="mb-6">
          <textarea
            id="textarea"
            rows="14"
            placeholder="Say some stuff"
            class="w-full p-2 outline-none"
          ></textarea>
        </div>
      </div>
    </div>
  </body>
</html>

<script>
  $(document).ready(function () {
    let text = $("#textarea").val();

    $.ajax({
      type: "GET",
      url: "/api/fetch?i=<%=i%>&t=<%=t%>",
      data: JSON.stringify({ text: text }),
      contentType: "application/json",
      success: function (data) {
        let text = data.text;
        $("#textarea").val(text).prop("readonly", true).attr("disabled", true);
      },
      error: function (xhr, status, error) {
        let text = xhr.responseJSON.message;

        $("#textarea")
          .val(`${text}`)
          .prop("readonly", true)
          .attr("disabled", true);
      },
    });
  });
</script>
