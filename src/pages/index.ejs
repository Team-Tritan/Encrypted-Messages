<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <title>encrypt</title>
    <style>
      body {
        font-family: sans-serif;
      }

      .max-w-3xl {
        box-shadow: 6px 4px 8px 9px rgba(128, 0, 128, 0.5);
      }
    </style>
  </head>
  <body class="bg-black text-white h-screen flex items-center justify-center">
    <div class="flex flex-col items-center">
      <div
        class="max-w-3xl p-8 bg-black rounded-lg border-2 border-purple-500 shadow-lg"
      >
        <h1 class="text-3xl font-semibold mb-4">Speak your mind.</h1>
        <p class="text-gray-500 mb-6">
          End to end encrypted. Only you and the receiver can read the message,
          deleted after the first read.
        </p>
        <div class="mb-6">
          <textarea
            id="textarea"
            rows="8"
            placeholder="Say some stuff"
            class="w-full p-2 bg-black outline-none"
          ></textarea>
        </div>
      </div>
      <button class="mt-10 bg-purple-900 text-white py-2 px-4 w-1/5 rounded-md">
        Send
      </button>
    </div>
  </body>
</html>

<script>
  $("button").click(function () {
    let text = $("#textarea").val();

    $.ajax({
      type: "POST",
      url: "/api/new",
      data: JSON.stringify({ text: text }),
      contentType: "application/json",
      success: function (data) {
        let url = `https://encrypting.tritan.gg/?i=${data.id}&t=${data.token}`;

        $("#textarea").val(url).prop("readonly", true).attr("disabled", true);

        $("button").remove();
      },
      error: function (xhr, status, error) {
        alert("Error: " + error);
      },
    });
  });
</script>
